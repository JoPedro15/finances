Módulo de Gráficos (src/utils/graphics.py):

Criar uma nova função de utilidade utilizando matplotlib e seaborn.

Desenhar um gráfico de barras comparativo claro (ex: Alocação Atual vs Alocação Alvo por ativo).

Gestão de Ficheiros Temporários (data/plots/):

Garantir que o diretório é criado automaticamente se não existir.

Guardar o gráfico gerado temporariamente como um ficheiro .png para envio.

Atualização da Integração Discord (src/infra/notifications/discord.py):

Ajustar o cliente de notificações para suportar o envio de ficheiros em anexo via requisições multipart (com webhooks do Discord).

Integração no Comando de Rebalanceamento (recommend-rebalance --notify):

Ligar a geração do gráfico ao fluxo do comando --notify, garantindo que o gráfico é anexado automaticamente ao alerta semanal.

Testes e Validação de Qualidade:

Escrever testes unitários para a criação de gráficos usando mocks de renderização (evitando gerar janelas gráficas durante os testes).

Assegurar que toda a pipeline de qualidade (make quality) passa sem falhas, mantendo as type annotations e a documentação técnica em inglês.